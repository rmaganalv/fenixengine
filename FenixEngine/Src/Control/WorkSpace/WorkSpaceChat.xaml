<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FenixEngine.Src.Control.WorkSpaceChat">

    <VerticalStackLayout Spacing="15">
        
        <!-- SECCIÓN DE ENTRADA / CONSULTA -->
        <Border Padding="15" 
                StrokeShape="RoundRectangle 10"
                BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}">
            
            <VerticalStackLayout Spacing="10">
                <Label Text="Instrucción de Sistema:" 
                       FontAttributes="Bold" />
                <Entry Text="{Binding SystemInstruction}" 
                       Placeholder="Ej: Eres un asistente experto en..." />

                <Label Text="Prompt / Consulta:" 
                       FontAttributes="Bold" 
                       Margin="0,5,0,0" />
                <Editor Text="{Binding UserQuery}" 
                        Placeholder="Escribe tu consulta aquí..."
                        AutoSize="TextChanges" 
                        HeightRequest="100" />

                <!-- Botón de Envío e Indicador de Carga -->
                <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10" Margin="0,5,0,0">
                    <Button Grid.Column="0"
                            Text="Enviar Consulta" 
                            Command="{Binding SendRequestCommand}" 
                            IsEnabled="{Binding IsNotBusy}" />
                    
                    <ActivityIndicator Grid.Column="1"
                                       IsRunning="{Binding IsBusy}" 
                                       IsVisible="{Binding IsBusy}" 
                                       VerticalOptions="Center" />
                </Grid>
            </VerticalStackLayout>
        </Border>

    </VerticalStackLayout>

</ContentView>
